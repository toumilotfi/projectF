version: '3.9'

services:
  db:
    image: postgres:17
    container_name: demo4
    environment:
      POSTGRES_USER: lotfi
      POSTGRES_PASSWORD: 92570533Lt@
      POSTGRES_DB: demo4
    ports:
      - "5333:5432"
    volumes:
      - db_data:/var/lib/postgresql/data
    restart: unless-stopped

volumes:
  db_data:
